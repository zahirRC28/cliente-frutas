import { Outlet }  from 'react-router-dom'

/**
 * Componente RendAni
 *
 * Renderiza un `Outlet` de React Router para rutas anidadas con animaciones.
 *
 * @component
 * @returns {JSX.Element} Outlet de React Router
 *
 * @example
 * <RendAni />
 */

export const RendAni = () => {
  return (
    <Outlet />
  )
}
